# Core Bot Framework (JobQueue requires the APScheduler extra)
python-telegram-bot[job-queue]>=20.0
python-dotenv>=1.0.0

# LLM Integration
httpx>=0.24.0  # HTTP client with retry support

# Utilities
pyyaml>=6.0
